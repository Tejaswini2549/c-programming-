#include <stdio.h>
#include <stdlib.h>

int main()
 {
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int a[50][50],b[50][50],mat[50][50];
        int i,j,k,n1,m1;
        scanf("%d%d",&n1,&m1);
        for(i=0;i<n1;i++)
        {
            for(j=0;j<m1;j++)
            {
                scanf("%d",&a[i][j]);
            }
        }
        int n2,m2;
        scanf("%d%d",&n2,&m2);
        for(i=0;i<n2;i++)
        {
            for(j=0;j<m2;j++)
            {
                scanf("%d",&b[i][j]);
            }
        }
        for(i=0;i<n1;i++)
        {
            for(j=0;j<m2;j++)
            {
                 mat[i][j]=0;
                 for(k=0;k<m1;k++)
                 {
                    mat[i][j]+=a[i][k]*b[k][j];
                 }

            }
        }
        for(i=0;i<n1;i++)
        {
            for(j=0;j<m2;j++)
            {
                printf("%d ",mat[i][j]);
            }
            printf("\n");
        }
    }

        
    return 0;
}
